package submission

/*func TestPopulateTestCases_Success(t *testing.T) {
	sp := defaultSubmissionProcessor{
		queue: make(chan SubmissionMetadata),
		quit:  make(chan int, 1)}

	testcases, err := sp.PopulateTestCases("volvo", "unittests")
	assert.Equal(t, nil, err)
	assert.Equal(t, []TestCase, testcases)
}*/
